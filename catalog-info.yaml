apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: my-repo-name          # kebab-case, must be unique in the catalog
  description: What this repo does
  annotations:
    github.com/murafay/mongo-k8s-stack   # owner/repo
spec:
  type: other
  lifecycle: experimental
  owner: platform
